PORT=3001
# Origines autorisées (CORS).
# Liste séparée par virgules. Le wildcard `*` est supporté pour les sous-domaines.
CLIENT_ORIGIN=http://localhost:5173,https://*.trycloudflare.com,https://*.ngrok-free.app,https://*.loca.lt
